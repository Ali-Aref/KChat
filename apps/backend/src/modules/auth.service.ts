import { IRegisterUserForm } from "@shared/schema/auth.schema";

export const registerUserService = (data: IRegisterUserForm) => {
  // throw new Error("Not implemented");
  console.log("here is the data", data)
};
